# 怎麼改這份課程雷達

這份文件同時是兩件事：**這個 repo 的操作說明**，以及**一次完整的 CI/CD 實戰練習**。

課程資料本身不複雜，正因為不複雜，很適合拿來把「改動 → 檢查 → 審查 → 上線」這條標準流程走熟。同一套流程換到正式系統上是一模一樣的。

---

## 先講為什麼不是用 Excel

我們原本可以開一個共用 Excel 丟在雲端硬碟。那樣會遇到四個問題：

| Excel 的做法 | 這裡的做法 |
| --- | --- |
| 誰改了什麼？打開檔案看不出來 | 每一次改動都有紀錄：誰、什麼時候、改了哪一行、為什麼 |
| 有人把日期打成 `2026/13/45`，沒人發現 | 機器自動檢查，格式錯了根本進不來 |
| 改壞了要靠「另存新檔_最終版_v3」救回來 | 任何一個歷史版本都能一秒還原 |
| 改完還要有人手動更新網頁 | 合併的那一刻自動上線，沒有人需要記得做這件事 |

這四件事就是 CI/CD 在解決的問題。跑一遍你就會有體感。

---

## 名詞先對齊

| 詞 | 白話 |
| --- | --- |
| **repo** | 這個專案的資料夾，含所有歷史紀錄 |
| **main** | 正式版分支。**線上網站長什麼樣，就是 main 現在的樣子** |
| **branch（分支）** | 從 main 拉出來的工作區。你在上面亂改都不會影響線上 |
| **commit** | 一次存檔，附一句「我改了什麼」 |
| **PR（Pull Request）** | 「我改好了，請看一下，沒問題就併進 main」的申請單 |
| **CI** | 機器自動幫你檢查（這裡是檢查課程資料格式） |
| **CD** | 檢查過了就自動上線 |
| **review** | 另一個人看過你的改動並按同意 |

**核心規則只有一條：不要直接改 main，一律走 PR。**

---

## 路線 A：在 GitHub 網頁上改（推薦新手，不用裝任何東西）

適合改錯字、改價格、改一兩個欄位。

1. 打開 [`data/courses.json`](../../blob/main/data/courses.json)
2. 按右上角的鉛筆圖示 ✏️（Edit this file）
3. 直接改內容
4. 拉到頁面最下面，你會看到一個綠色區塊：
   - 上面那格填**改動摘要**（例如：`更新 CPC 內稽班早鳥價`）
   - 選擇 **"Create a new branch for this commit and start a pull request"** ← 這格很重要，別選上面那個直接提交到 main
   - 按 **Propose changes**
5. 下一頁按 **Create pull request**
6. 等 CI 跑完（見下面「看懂紅綠燈」），找一個人 review
7. 被 approve 之後按 **Merge pull request**
8. 約一分鐘後線上網站就更新了

---

## 路線 B：在自己電腦上改（改的東西比較多時）

只要做一次的設定：

```bash
git clone https://github.com/<帳號>/iso-cert-radar.git
cd iso-cert-radar
```

之後每次改動都重複這五步：

```bash
# 1. 先把別人的改動拉下來，避免衝突
git checkout main
git pull

# 2. 開一條新分支。名字用「動作/內容」，讓人一眼看懂
git checkout -b add/sgs-27001-1215

# 3. 改 data/courses.json，然後在本機自己先檢查一次
node scripts/validate.mjs

# 4. 存檔並推上去
git add data/courses.json
git commit -m "新增 SGS 12/15 27001 內稽班"
git push -u origin add/sgs-27001-1215

# 5. 終端機會印出一個網址，點進去開 PR
```

想在本機看網頁長什麼樣：

```bash
npm start          # 或 python3 -m http.server 8000
```

然後打開 http://localhost:8000 。

> ⚠️ **不要用滑鼠雙擊 index.html 打開。** 瀏覽器的安全限制會擋掉本機讀取 JSON 檔，畫面會是空的。一定要起一個本機伺服器。

---

## 看懂紅綠燈

開完 PR，頁面下方會出現檢查結果：

- 🟡 **黃點** — 機器正在跑，等 30 秒左右
- ✅ **綠勾** — 資料格式沒問題，可以找人 review 了
- ❌ **紅叉** — 有錯，**這時候不要硬合併**

紅叉的時候，點 **Details** 進去看，最下面會有中文的錯誤說明，長這樣：

```
✗ 檢查沒過：31 筆課程裡有 1 個問題

  1. courses[7] (id: leadership-42001-1116)
     問題：報名截止日 (2026-12-01) 晚於開課日 (2026-11-16)
     怎麼修：報名截止日應該在開課之前。
```

它會直接告訴你是哪一筆、哪裡錯、怎麼修。修好之後再 commit 一次，CI 會自動重跑，不用重開 PR。

---

## 每筆課程的欄位

必填：

| 欄位 | 說明 | 範例 |
| --- | --- | --- |
| `id` | 唯一代號，小寫英文+數字+連字號。慣例是 `主辦-標準-開課日` | `cpc-27001-ia-0914` |
| `start` / `end` | 開課日 / 結訓日，**一定是 `YYYY-MM-DD`** | `2026-09-14` |
| `when` | `weekday`（平日要請假）／`weekend`（六日）／`tbd`（待確認） | `weekday` |
| `days` | 給人看的時數描述 | `2 天 / 14 小時` |
| `code` | 標準代號 | `ISO/IEC 27001:2022` |
| `level` | 資格等級 | `內部稽核員` |
| `cats` | 分類陣列，決定會被哪些篩選按鈕撈到 | `["27001", "short"]` |
| `title` `org` `place` | 課名、主辦單位、地點 | |
| `priceNote` | 價格的補充說明 | `3 人以上 4,725／人` |

選填：

| 欄位 | 說明 |
| --- | --- |
| `price` | 數字。免費填 `0`，價格未公開填 `null`（會顯示「洽主辦」）。**不要寫成 `"8,550"` 或 `"8550元"`** |
| `was` | 原價，會顯示成刪除線。必須大於 `price` |
| `deadline` | 報名截止日，會觸發「報名剩 N 天」的倒數 |
| `url` | 報名頁網址。沒有就填 `null` |
| `note` | 補充說明，建議 120 字以內 |
| `alert` | `true` 會讓 note 變成橘色警示（資料待確認、有陷阱時用） |
| `pick` | `true` 會標上「建議優先」 |
| `cert` | 發證等級。**不填的話系統會自己從 `level` 推斷**，推不準時才手動指定 |

`cats` 可用的值：`27001` `27701` `cloud` `42001` `pdpa` `short` `free`

`cert` 可用的值：`irca`（IRCA 國際登錄）`iso`（ISO 稽核員證書）`pro`（個資管理師）`unknown`（發證待確認）`none`（不發證照）

> 📌 **`cert` 這個欄位是整份資料最重要的判斷。** 市面上很多課名都叫「內部稽核員」，但有的只發結業證書，拿去應付驗證是無效的。不確定就填 `unknown`，不要猜 `iso` — 標錯會害人報錯課。

改完資料記得把 `meta.updated` 換成今天的日期。

---

## Review 怎麼做

這個 repo 設定成**至少要一個人 approve 才能合併**。輪到你 review 別人的 PR 時，看三件事：

1. **綠勾有沒有亮** — 沒亮就先請對方修，不用細看內容
2. **資料對不對** — 點開 PR 裡的來源網址，比對價格和日期是不是真的那樣
3. **`cert` 標得對不對** — 這是最容易出錯也最有殺傷力的欄位

覺得可以就按 **Approve**；有疑問就在那一行留言，對方會收到通知。

---

## 常見狀況

**「我不小心直接改到 main 了」**
沒事，這個 repo 的 main 有保護，直接推會被擋。真的推進去了就開一個 PR 改回來。

**「PR 顯示有衝突（conflict）」**
代表你開分支之後，別人也改了同一段。最快的解法：把你的改動記下來，刪掉分支重開一條新的，重改一次。

**「我想加一個新的篩選分類」**
要同時改三個地方：`scripts/validate.mjs` 的 `CATS` 白名單、`index.html` 的篩選按鈕、以及資料裡對應課程的 `cats`。這種改動請在 PR 描述裡講清楚。

**「網站好像沒更新」**
去 **Actions** 分頁看最新一次「部署到 GitHub Pages」有沒有跑完。跑完了還是舊的，按 `Cmd+Shift+R` 強制重新整理。

---

## 只是想回報，不想自己動手改

開一個 [Issue](../../issues/new/choose)，選「新增課程梯次」，把知道的資訊填進去就好，會有人幫你補上。
